const onLogin = (username, password) => {
    // Logic for handling login
    // Example: Authenticate user, set session, etc.
    console.log('Login function called with username:', username);
  };
  
  export default onLogin;
  